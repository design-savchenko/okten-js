'use strict';

let x = 1;
let a = 0;
let b = -3;

const correctAnswer = 'Right!';
const wrongAnswer = 'Wrong!';

if (x !== 0) {
    console.log(`${correctAnswer}`);
} else {
    console.log(`${wrongAnswer}`);
}

if (a !== 0) {
    console.log(`${correctAnswer}`);
} else {
    console.log(`${wrongAnswer}`);
}

if (b !== 0) {
    console.log(`${correctAnswer}`);
} else {
    console.log(`${wrongAnswer}`);
}